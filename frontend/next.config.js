/*
 * Next.js Configuration
 *
 * Configures the Next.js 14 app for the BioDreamer frontend:
 *   - API proxy: rewrites /api/* requests to the FastAPI backend (http://localhost:8000)
 *   - Image optimisation: allow images from Hugging Face Hub
 *   - Webpack: configure for Mol* (3D protein viewer) WASM modules
 *   - Output: standalone for Docker deployment
 */
