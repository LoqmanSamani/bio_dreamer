/**
 * BlogPostCard — Summary Card for Blog Index.
 *
 * Purpose:
 *   Displays a single blog post preview in the blog index page.
 *
 * Props:
 *   - slug: string            — URL slug (e.g. "2026-03-19-introducing-biodreamer")
 *   - title: string           — post title
 *   - date: string            — ISO date string
 *   - author: string          — author name
 *   - tags: string[]          — topic tags
 *   - summary: string         — 1-2 sentence preview
 *
 * Visual:
 *   - Card with subtle border, hover shadow effect
 *   - Title as <h3> link to /blog/[slug]
 *   - Date + author in muted text below title
 *   - Tags as small rounded badges (colour-coded by topic)
 *   - Summary paragraph
 *   - "Read more →" link
 */
