apply the modifications mentioned below to `plans/phd_proposal.tex`:

- literature review section is too long with too many subsection, compact it into two to three pages with two to three subssction (combine subsection into new subsection with a broader domain)
- change the citation format from [Author name, publication year] to [number], e.g., [2].
- title and author info should be in one page (right now abstract is part of the same page which is not proper), add my email and webpage to this page: email: samaniloqman91@gmail.com, webpage: https://loqmansamani.github.io
- move contents to right below title (flip it with abstract).
- in methodology and also in appendix, the exact number of layers and some parameters are specified (e.g., three layers of GVP-GNN, or the exact mutation embedding dim = 128) which should not be specified, cause it is a proposal and not the exact experimental instruction.
- diffusion papers are not cited att all, which should be cited corrctly. e.g., stable diffusion is used without citing LDMs.
- add a table for all math-signs and equations in appendix (e.g., L_diff: denoising score-matching objective)
- methodology section contains almost no equation so each subsection of it shuold be properly linked to the corresponding subsection in appendix.
- FusionMLP (combines sequence and structure embedding info and project them to latent space, see `equation_architecture.md` for more info) is not mentioned in appendix, add it 
- is the diffusion equation mentioned in appendix from ddpm??, or it is a general way to define diffusion process, cite and mention the source.
- mention algorithms in appendix inside the proposal in the related section, so readers can refer to those algorithms to better understanding.
- currently citations are limited mostly to literature review, add citation also to the other parts of the proposal if when it is necessary
